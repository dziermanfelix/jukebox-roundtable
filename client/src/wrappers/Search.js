import styled from 'styled-components';

const Wrapper = styled.section`
  overflow: scroll;
  display: block;
  .search-results {
    margin: .1em;
    overflow: scroll;
    width: 100%;
    height: 50%;
  }
`;

export default Wrapper;
